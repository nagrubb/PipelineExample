Jenkinsfile (Declarative Pipeline)

pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
            }
        }
    }
}
