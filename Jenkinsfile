pipeline {
  agent { label 'master' }
    stages {
      stage('build') {
        steps {
          sh 'npm --version'
        }
      }
      stage('test') {
        steps {
          sh 'npm --version'
        }
      }
      stage('package') {
        steps {
          sh 'npm --version'
        }
      }
    }
  }
}
